from django.apps import AppConfig


class PowerUpConfig(AppConfig):
    """Configuration for the Power-Up corporate site app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "power_up"
    verbose_name = "Power-Up Corporate Site"
